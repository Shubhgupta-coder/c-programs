#include <stdio.h>

int main() {
	float a=0.7;
	if(a<0.7)
	{
	    printf("abes is a good college");// here if condition satisfy because a is a float variable and 0.7 is a double constant. the float variable is less than the double constant//
	}     
	else
	{
	    printf("abes is not a good college");
	}
	return 0;
}